export const metadata = {
  title: "Project1 Authenticate",
  description: "Next.js frontend for authenticate project"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
